apiVersion: v1
data:
  config: |
    [auth]
    type = htpasswd
    htpasswd_filename = /etc/radicale/users
    htpasswd_encryption = autodetect
    [sharing]
    type = map
    storage_type = files
kind: ConfigMap
metadata:
  name: config
