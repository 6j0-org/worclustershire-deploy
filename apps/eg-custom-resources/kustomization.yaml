apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Installed using helm, so namespace is metallb, not metallb-system.
namespace: envoy-gateway-system

resources:
  - gatewayclass.yaml
  - gateway.yaml
