# https://fluxcd.io/flux/components/helm/helmreleases/
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg-grafana
spec:
  # HelmRepository example
  # chart:
  #   spec:
  #     chart: cnpg-grafana
  #     version: 0.0.0
  #     sourceRef:
  #       kind: HelmRepository
  #       name: prometheus-community

  # OCIRepository example
  # chartRef:
  #   kind: OCIRepository
  #   name: devopscoop-app
  #   # TODO: Is namespace needed?
  #   # namespace: cnpg-grafana
  interval: 10m
  valuesFrom:
    - kind: ConfigMap
      name: cnpg-grafana-values
    - kind: Secret
      name: cnpg-grafana-secrets
  chartRef:
    kind: OCIRepository
    name: cnpg-grafana-cluster
