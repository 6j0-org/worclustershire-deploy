# https://fluxcd.io/flux/components/helm/helmreleases/
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: chorechart
spec:
  interval: 10m
  valuesFrom:
    - kind: ConfigMap
      name: chorechart-values
    - kind: Secret
      name: chorechart-secrets
  chartRef:
    kind: OCIRepository
    name: devopscoop-app
