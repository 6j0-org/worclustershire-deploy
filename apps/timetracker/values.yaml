replicaCount: 1
image:
  repository: ghcr.io/andreajtucker/timetracker
  tag: dev-c21c8d2-1779830961 # {"$imagepolicy": "flux-system:timetracker:tag"}
service:
  port: 3001
httpRoute:
  enabled: true
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - timetracker.6j0.org
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
env:
  - name: DATABASE_URL
    valueFrom:
      secretKeyRef:
        name: timetracker-db-app
        key: uri
