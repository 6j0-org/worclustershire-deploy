replicaCount: 1
image:
  repository: ghcr.io/andreajtucker/timetracker
  tag: dev-c1be35f-1779840724 # {"$imagepolicy": "flux-system:timetracker:tag"}
service:
  port: 3001
httpRoute:
  enabled: true
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - timetracker.6j0.org
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
env:
  - name: DATABASE_URL
    valueFrom:
      secretKeyRef:
        name: timetracker-db-app
        key: uri
