# https://fluxcd.io/flux/components/helm/helmreleases/
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: youcandoithealth
spec:
  # HelmRepository example
  # chart:
  #   spec:
  #     chart: youcandoithealth
  #     version: 0.0.0
  #     sourceRef:
  #       kind: HelmRepository
  #       name: prometheus-community

  # OCIRepository example
  # chartRef:
  #   kind: OCIRepository
  #   name: devopscoop-app
  #   # TODO: Is namespace needed?
  #   # namespace: youcandoithealth
  interval: 10m
  valuesFrom:
    - kind: ConfigMap
      name: youcandoithealth-values
    - kind: Secret
      name: youcandoithealth-secrets
  chartRef:
    kind: OCIRepository
    name: devopscoop-app
