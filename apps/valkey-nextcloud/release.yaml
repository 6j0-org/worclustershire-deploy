# https://fluxcd.io/flux/components/helm/helmreleases/
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: valkey-nextcloud
spec:
  # HelmRepository example
  # chart:
  #   spec:
  #     chart: valkey-nextcloud
  #     version: 0.0.0
  #     sourceRef:
  #       kind: HelmRepository
  #       name: prometheus-community

  # OCIRepository example
  # chartRef:
  #   kind: OCIRepository
  #   name: devopscoop-app
  #   # TODO: Is namespace needed?
  #   # namespace: valkey-nextcloud
  interval: 10m
  valuesFrom:
    - kind: ConfigMap
      name: valkey-nextcloud-values
    - kind: Secret
      name: valkey-nextcloud-secrets
  chartRef:
    kind: OCIRepository
    name: valkey-valkey
