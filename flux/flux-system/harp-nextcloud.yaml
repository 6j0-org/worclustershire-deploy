apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: harp-nextcloud
  namespace: flux-system
spec:
  dependsOn:
    - name: nextcloud
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: apps/harp-nextcloud
  prune: true
  wait: true
  timeout: 5m0s
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  suspend: false
